node_modules/
test-results/
playwright-report/

# tool state, not project content
.mimosa/
.claude/
.zcode/

.DS_Store
*.log
__pycache__/
*.pyc

# built by build/build.py, and by the deploy workflow. Not committed:
# two branches that each build correctly merge into a file matching neither.
site/index.html
site/index.de.html

# written by build/linkcheck.py before every agent run — per-run artifact
linkcheck-report.md

# written and read inside one cross-check run — per-run artifacts
auftrag.json
befund-*.json
vergleich.md
vergleich.json
